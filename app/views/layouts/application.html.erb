<!DOCTYPE html>
<html>
  <head>
    <title>DinnerParty</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.6.0/css/bulma.min.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />

  </head>

  <body>

<header class="header">
  <div class="container">
    <div class="content has-text-centered">
  <%if session[:user_id] != nil%>
    <span class="icon is-small"><i class="fa fa-home"></i></span><span><%=link_to 'MyProfile', my_profile_path%> </span>
    <span class="icon is-small"><i class="fa fa-book"></i></span><span><%= link_to 'Recipes', recipes_path %>  </span>
    <span class="icon is-small"><i class="fa fa-sign-out"></i></span><span><%=link_to 'Logout', logout_path, method: :delete%></span>


    <% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: name %>
    <% end -%>

  <%else%>
    <%=link_to 'Login', login_path %>
    <%=link_to 'Signup', new_user_path %>
  <%end%>

</div>
</div>
<br>
</header>
    <%= yield %>

  </body>
</html>
